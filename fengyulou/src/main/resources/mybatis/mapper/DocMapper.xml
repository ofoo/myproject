<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.guoguo.fengyulou.dao.DocDao">

    <resultMap id="DocResult" type="Doc">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="markdownText" column="markdown_text"/>
        <result property="htmlText" column="html_text"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createUserId" column="create_user_id"/>
    </resultMap>

    <sql id="fengyulou_doc_columns">
		fengyulou_doc.id,
		fengyulou_doc.name,
		fengyulou_doc.markdown_text,
		fengyulou_doc.html_text,
		fengyulou_doc.create_time,
		fengyulou_doc.update_time,
		fengyulou_doc.create_user_id
	</sql>

    <sql id="fengyulou_doc_properties">
		#{id},
		#{name},
		#{markdownText},
		#{htmlText},
		now(),
		now(),
		#{createUserId}
	</sql>

    <insert id="addDoc" useGeneratedKeys="true" keyProperty="id">
        insert into fengyulou_doc(<include refid="fengyulou_doc_columns"/>)
        value(<include refid="fengyulou_doc_properties"/>)
    </insert>

    <update id="updateDoc">
        update fengyulou_doc set
        `markdown_text`=#{markdownText},
        `html_text`=#{htmlText},
        update_time=now()
        where id=#{id} and create_user_id=#{createUserId}
    </update>

    <update id="updateDocNameByIdAndCreateUserId">
        update fengyulou_doc set
        `name`=#{name}
        where id=#{id} and create_user_id=#{createUserId}
    </update>

    <select id="getDocByCreateUserId" resultMap="DocResult">
        select
        fengyulou_doc.id,
		fengyulou_doc.markdown_text,
		fengyulou_doc.html_text
        from fengyulou_doc
        where create_user_id=#{value}
    </select>

    <select id="getDocList" resultMap="DocResult">
        select id,`name`,markdown_text,html_text from fengyulou_doc
        <where>
            create_user_id=#{createUserId}
            <if test="name!=null">
                and `name` like CONCAT('%',#{name},'%')
            </if>
        </where>

    </select>

    <select id="getDocByIdAndCreateUserId" resultMap="DocResult">
        select id,`name`,markdown_text,html_text from fengyulou_doc where id=#{id} and create_user_id=#{createUserId}
    </select>

    <select id="getDocById" resultMap="DocResult">
        select id,`name`,markdown_text,html_text from fengyulou_doc where id=#{id}
    </select>
</mapper>